{
  "project_options": {
    "proxy": {
      "request_listeners": [
        {
          "certificate_mode": "per_host",
          "listen_mode": "loopback_only",
          "listener_port": {{ burp_proxy_port }},
          "running": true
        }
      ]
    }
  }
}
